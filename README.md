# oemof-tutorials

A flexible framework for energy system modeling and scenario analysis using **oemof.solph**. This repository allows creating multiple energy system scenarios (e.g., PV only, PV + battery) with configurable components and solver settings.  

---